# Aseprite Action Viewer Manual Regression Checklist

자동 검사 후 실제 Windows GUI에서 수행할 항목입니다. 각 항목은 사용자 애셋의 복사본이나 별도 테스트 프로젝트로 확인하세요.

## 시작과 종료

- [ ] `run.bat`으로 앱 실행 — 창이 열리고 빈 프로젝트가 오류 없이 표시된다.
- [ ] 빈 프로젝트에서 종료 — 앱이 정상 종료되고 저장 실패가 있으면 한 번만 안내한다.
- [ ] 쓰기 권한이 없는 테스트 폴더에서 저장 — 기존 JSON은 보존되고 권한 확인 메시지가 표시된다.

## EX 1

- [ ] `EX 1` 클릭 — `resources/examples/shared/sources`의 Cailin/Nisariel을 내보내고 PLAYER/NPC_1 프로필 두 개를 만든다.
- [ ] 플레이어와 애니메이션 — 복구된 명시적 태그 매핑이 화면에 재생된다.
- [ ] 공격과 대시 — 매핑이 존재하는 동작은 실행되고 없는 태그는 앱을 중단하지 않는다.
- [ ] 카메라와 지형 — 세로 오프셋 `-120`, 플랫폼 6개와 솔리드 박스 2개가 적용된다.
- [ ] 배경 — `로비 컨셉94 (1).png` 한 레이어가 위치 `Y=-130`, 배율 `2.0`, 패럴랙스 `0.9862068965517241`로 표시된다.
- [ ] NPC/프랍 — NPC_1 인스턴스가 준비되고 프랍은 생성되지 않는다. 자동 프랍은 과거 프리셋에 없었음이 정상이다.

## EX 2

- [ ] `EX 2` 클릭 — Cailin PLAYER와 Nisariel NPC_1 프로필 두 개를 만든다.
- [ ] EX 1과 비교 — 세로 오프셋 `-100`, 배경색 `[15, 15, 18]`, 6개 패럴랙스 환경이 적용된다.
- [ ] 교대 — `T`를 누르면 PLAYER와 NPC_1 프로필이 교대되고 퇴장/입장 태그가 없더라도 중단되지 않는다.
- [ ] 레이어 순서 — `00.png`, `01.png`, `# 2번_완성본.png`, `# 3번_완성본.png`, `# 4번_완성본.png`, `레이어 3.png` 순서다.
- [ ] 패럴랙스 — 카메라를 좌우로 이동하면 계수 `0`, `0.05`, `0.06`, `0.5344827586206895`, `0.703448275862069`, `1.0`에 따라 서로 다르게 이동한다.
- [ ] 반복 — 마지막 3개 레이어만 Loop X가 활성화되고, `# 2번_완성본.png`는 알파 `125`로 표시된다.

## 파일 누락과 복구

- [ ] 예제 리소스 복사본 하나를 별도 테스트 복사에서 임시로 다른 위치에 둔 뒤 EX 버튼 클릭 — 누락 파일이 표시되고 현재 상태가 유지된다.
- [ ] 리소스 해시가 달라짐 — 손상/변경된 파일명이 안내되고 현재 상태가 유지된다.
- [ ] 리소스 복원 후 재시도 — 모든 내보내기·태그·배경 로딩이 성공한 뒤에만 예제 상태가 적용된다.
- [ ] 프로젝트 소스 한 개 누락 — 누락 목록을 한 번 안내한 뒤 해당 파일의 대체 경로를 묻는다.
- [ ] 프로젝트 소스 여러 개 누락 — 파일별로 한 번씩 묻고 하나라도 취소하면 전체 로드를 취소해 기존 상태를 유지한다.
- [ ] 같은 누락 경로가 반복됨 — 같은 파일은 한 번만 선택한다.
- [ ] 배경 이미지 누락 — 누락 배경을 한 번에 요약하고 `SETUP > BG IMAGE` 재지정을 안내한다.

## 프로젝트 경로

- [ ] 새 프로젝트 저장 후 재실행 — 소스, 프로필, 플랫폼, 단축키가 복원된다.
- [ ] 프로젝트 폴더 이동 — 같은 폴더 구조의 상대 경로 애셋이 복원된다.
- [ ] 기존 절대 경로 프로젝트 — 기존 경로가 존재하면 그대로 불러온다.
- [ ] 공백·한글 포함 경로 — 저장, 불러오기, Aseprite 내보내기가 정상 동작한다.
- [ ] 손상된 JSON — 현재 상태와 원본 파일을 유지하고 오류 안내를 표시한다.

## Aseprite 연동

- [ ] Steam/Program Files 자동 탐색 — 별도 설정 없이 설치 경로를 찾는다.
- [ ] 직접 경로 지정 — 선택한 `Aseprite.exe`가 `config.json`에 저장된다.
- [ ] 잘못된 기존 경로 — 자동 탐색 결과를 사용한다는 안내가 한 번 표시된다.
- [ ] CLI 실패·타임아웃·결과 누락 — 앱은 유지되고 요약 메시지와 `ase_debug.log` 상세 정보가 남는다.

## 주요 기능

- [ ] 좌우 이동, 점프, 대시, 기본 공격과 콤보가 정상 전환된다.
- [ ] 교대 중 기존 공격 큐가 안전하게 종료된다.
- [ ] `+ NPC`, `+ PROP` 생성 후 한 프레임 이상 정상 갱신·렌더링된다.
- [ ] 플랫폼과 솔리드 박스를 추가·이동·크기 조절·삭제할 수 있다.
- [ ] 배경 이미지 추가와 패럴랙스, 반복 설정이 저장 후 복원된다.
- [ ] 프로젝트 저장 후 앱 재실행에서도 변경이 유지된다.

## 레이어 표시

### 목록과 순서

- [ ] Aseprite 원본과 LAYERS 목록의 전체 항목 수가 같다.
- [ ] Aseprite 아래→위 스택 순서와 LAYERS 순서가 같고 이름순으로 재정렬되지 않는다.
- [ ] 최하단과 최상단 레이어가 모두 표시된다.
- [ ] 그룹과 2단계 이상 자식이 원래 이름과 들여쓰기로 표시된다.
- [ ] 빈 레이어와 원래 숨긴 레이어도 목록에 남는다.
- [ ] 30개 이상 레이어에서 스크롤해 마지막 행까지 접근할 수 있다.
- [ ] 레이어를 줄인 뒤 탭에 다시 들어와도 스크롤이 빈 영역에 고정되지 않는다.

### 토글과 렌더

- [ ] 일반 레이어를 OFF하면 해당 레이어만 렌더에서 사라지고 행은 목록에 남는다.
- [ ] 다시 ON하면 원본 렌더와 동일하게 복원된다.
- [ ] 같은 소스의 첫 번째 동명 레이어만 OFF하면 두 번째는 ON과 렌더를 유지한다.
- [ ] 두 번째 동명 레이어만 OFF해도 첫 번째 상태와 독립적으로 동작한다.
- [ ] 중첩 그룹과 서로 다른 소스의 동명 레이어 상태가 섞이지 않는다.
- [ ] 그룹 OFF/ON이 자식의 로컬 토글 상태를 임의로 바꾸지 않는다.
- [ ] 토글 전후 목록 개수와 순서가 변하지 않는다.

### 새로고침과 실패

- [ ] F5 후 기존 UUID 레이어의 ON/OFF와 순서가 유지된다.
- [ ] Aseprite에서 새 레이어를 추가하면 기본 원본 표시 상태로 나타난다.
- [ ] 삭제한 레이어 행은 사라지고 다른 레이어 상태에는 영향이 없다.
- [ ] 레이어 이름을 바꿔도 UUID가 유지되면 ON/OFF가 유지된다.
- [ ] 새로고침 실패 시 기존 목록과 마지막 정상 렌더가 그대로 유지된다.
- [ ] 토글과 F5가 원본 `.aseprite`의 수정 시각이나 해시를 바꾸지 않는다.

## DEAD_LOOP NPC

- [ ] `Dead_(Loop)` 태그가 자동으로 `DEAD_LOOP`에 매핑되고 일반 `DEAD`와 구분된다.
- [ ] 해당 NPC가 죽은 위치와 방향에 시체가 유지되고 애니메이션이 반복된다.
- [ ] 비반복 DEAD_LOOP는 마지막 프레임에서 정지한다.
- [ ] 시체는 다시 피격되거나 플레이어/다른 NPC의 전투 대상으로 선택되지 않는다.
- [ ] 다른 NPC의 이동과 전투는 정상적으로 계속된다.
- [ ] 새 프로젝트·예제 적용 또는 연결 소스 삭제 시 시체가 제거된다.

## Parts NPC 사망

- [ ] DEAD_LOOP 없이 유효한 Parts가 있는 NPC는 Slice 모양 그대로 정밀 파편을 만든다.
- [ ] NPC의 좌우 방향에 맞게 파편 이미지와 월드 오프셋이 반전된다.
- [ ] 정밀 파편을 만든 뒤 NPC 본체가 제거되고 3×3 파편은 추가 생성되지 않는다.

## Parts 없는 NPC 사망

- [ ] DEAD_LOOP와 Parts가 없으면 기존 사망 프레임 3×3 파편이 생성된다.
- [ ] NPC 본체가 제거되고 3×3 생성 실패 시에도 단색 폴백으로 앱이 계속 동작한다.
- [ ] Parts가 없으므로 SAVE는 비활성이고, 눌렀을 때 이유가 표시된다.

## NPC/PROP Slice 저장

- [ ] `SETUP > NPCS`와 `SETUP > PROPS`의 프로필 행에 `SAVE` 버튼이 표시된다.
- [ ] SAVE를 누르기 전부터 각 행에 `P:n / FX:n / SAVE:ON|OFF` 자동 분석 상태가 표시된다.
- [ ] NPC 피격 시 유효한 Particles Slice 이미지가 사용되고, 없거나 비어 있으면 기존 기본 파티클이 사용된다.
- [ ] 동일 소스를 NPC와 PROP이 참조하면 표시 개수와 AUTO Slice 목록이 같다.
- [ ] NAME 저장 후에도 NPC 피격·사망 런타임 결과는 AUTO 기준으로 유지된다.
- [ ] F5 성공 후 기존 NPC를 다시 만들지 않아도 새 개수가 반영되고, F5 실패 시 이전 개수가 유지된다.
- [ ] 유효한 소스·Parts 태그·불투명 Slice가 있을 때만 SAVE가 활성화된다.
- [ ] Parts가 없거나 비어 있거나 Particles만 있으면 비활성 이유가 UI에 표시된다.
- [ ] AUTO를 선택하면 기본 이름 `Slice 1~n`도 Parts/Particles 태그의 전체 프레임 픽셀로 분류된다.
- [ ] NAME을 선택하면 기존 `particle` 이름 기반 필터 결과가 유지된다.
- [ ] 태그 순서와 무관하게 `<프로필>_Parts_01.png`와 `<프로필>_Particle_01.png`부터 각각 번호가 시작된다.
- [ ] 전체 태그 프레임이 빈 Slice는 제외되고, 뒤 프레임에 픽셀이 있으면 저장된다.
- [ ] 한 Slice가 양쪽 태그에서 유효하고 이름 힌트가 없으면 양쪽 결과를 보존한다.
- [ ] 완료 안내에 저장·건너뜀·실패 수와 출력 폴더가 표시된다.
- [ ] 대상 태그나 Slice가 없으면 성공으로 표시하지 않고 이유를 안내한다.
- [ ] 같은 파일명이나 기존 파일이 있으면 `_2`, `_3` 이름을 사용하고 기존 파일을 보존한다.
- [ ] 폴더 선택을 취소하면 파일과 프로젝트 상태가 바뀌지 않는다.
- [ ] 같은 프로필 이름과 소스를 NPC/PROP이 참조하면 새 폴더의 파일명 목록과 PNG 해시가 같다.
- [ ] 3×3 런타임 파편은 SAVE 결과에 포함되지 않는다.

## 소스 삭제

- [ ] NPC 소스를 삭제하면 직접 연결된 프로필과 NPC 인스턴스가 함께 제거된다.
- [ ] PROP 소스를 삭제하면 직접 연결된 프로필과 PROP 인스턴스가 함께 제거된다.
- [ ] 첫 번째·중간·마지막 소스를 각각 삭제해도 뒤쪽 소스와 매핑이 올바른 인덱스를 가리킨다.
- [ ] 삭제 결과 안내에 연결 프로필·NPC·PROP 제거 수가 표시된다.
- [ ] 남은 프로필과 소스가 update/render에서 오류 없이 동작한다.
- [ ] 프로젝트 저장 후 재실행하면 NPC/PROP 분류와 SAVE 행이 유지된다.

## 배포 실행 파일

- [ ] `ase_viewer.exe --check`, `--check-aseprite`, `--check-gui`가 종료 코드 0을 반환한다.
- [ ] 실행 파일을 다른 현재 작업 폴더에서 실행해도 앱 루트 기준 경로를 사용한다.
- [ ] 실행 파일 옆에 `resources/examples`를 함께 둔 내부 배포본에서 EX 1·EX 2가 로드된다.
- [ ] PyInstaller 실행 중 `pkg_resources` 사용 중단 예정 경고는 현재 알려진 빌드 경고이며 실행 실패로 취급하지 않는다.

## v0.5.7.5 교체 / 키 가이드 / NPC 회수

- [ ] 같은 위치에서 `T` 교체 시 새 Player가 이전 캐릭터의 `Swap_Exit` 연출보다 전면에 보인다.
- [ ] 교체 연출이 끝난 뒤 이전 캐릭터가 배치 NPC나 Partner로 중복 등록되지 않는다.
- [ ] 캐릭터 가이드에 기본 `E 합격기`가 보이고 SYNERGY 키를 바꾸면 즉시 새 키가 표시된다.
- [ ] 앱 가이드의 `O`가 `1프레임 진행`으로 보이고 툴팁이 일시정지 중 한 프레임 진행을 설명한다.
- [ ] 살아 있는 NPC와 시체가 함께 있을 때 `G`를 누르면 살아 있는 NPC만 Player 주변으로 이동한다.
- [ ] 시체만 있을 때 `G`를 눌러도 위치가 바뀌거나 오류가 나지 않고 대상 없음 안내가 표시된다.

## v0.5.8 OPTIONS 섹션

- [ ] OPTIONS 상단에 Quick/Input/AI/Background/View/Advanced 6개 섹션이 보이고 현재 섹션이 파란색으로 구분된다.
- [ ] 다른 sidebar로 이동했다 돌아와도 현재 세션의 설정 섹션이 유지되고 재시작 기본값은 Quick이다.
- [ ] Quick에서 현재 선택, 처음 사용 흐름, F5/F10/H와 하단 가이드 안내를 확인할 수 있다.
- [ ] Input에서 Attack/Dash/Jump/Swap/Skill/Synergy 키를 편집하고 Character/App 설명을 찾을 수 있다.
- [ ] Fast Action이 캐릭터 조작 범위로 설명되고 controller가 구현된 기능처럼 표시되지 않는다.
- [ ] AI / Combat에서 NPC 행동, Intro 다시 재생, ComboAttack과 H 안내를 찾을 수 있다.
- [ ] Background에서 BG layer, parallax, X/Y offset, gizmo, Shift 축 잠금과 undo/redo 안내를 찾을 수 있다.
- [ ] 배경 레이어가 없거나 선택 인덱스가 잘못되어도 OPTIONS가 중단되지 않는다.
- [ ] View / Debug에서 P/O/[ ]/F10/H와 우클릭 드래그/F 카메라 안내를 찾을 수 있다.
- [ ] Advanced에서 상단 project 도구와 F5 안내, 기존 PROP/물리 설정을 찾을 수 있다.
- [ ] 섹션 전환 후 이전 섹션의 보이지 않는 버튼·슬라이더가 클릭되지 않는다.
- [ ] 최소 창과 깊은 스크롤에서 섹션 버튼, 본문, tooltip이 header와 겹치지 않는다.
- [ ] 한국어/영어 전환 후 긴 제목과 도움말이 잘리거나 겹치지 않는다.
- [ ] 하단 Character/App guide, Synergy, O 1프레임 진행과 F10 overlay가 그대로 유지된다.

## v0.5.8.1 Header / OPTIONS 3탭

- [ ] 상단 순서가 `태그 등록 / 배치 / 리소스 / 옵션`이고 네 버튼이 모두 기존 mode를 연다.
- [ ] 활성 버튼을 다시 눌러도 mode가 유지되며 empty project에서도 모두 클릭된다.
- [ ] OPTIONS 탭이 한 줄 `조작·앱 / 장면·전투 / 화면·배경` 3개만 표시된다.
- [ ] Quick과 Advanced가 사라졌지만 기존 설정 카테고리는 하나도 사라지지 않았다.
- [ ] 조작·앱에서 언어, Character key map, Synergy, App shortcut, Fast Action/controller 안내를 찾을 수 있다.
- [ ] 장면·전투에서 NPCS, 행동, Intro, Combo, Despawn/corpse 안내, PROPS와 PHYSICS를 찾을 수 있다.
- [ ] 화면·배경에서 BG, parallax, gizmo/history, CAMERA, LAYERS, VFX와 F10/H/우클릭/F를 찾을 수 있다.
- [ ] 한/영 전환 즉시 세 탭 제목이 바뀌고 최소 창에서도 버튼이 겹치지 않는다.
- [ ] 탭 전환 후 이전 탭의 scroll·key 입력·숫자 입력과 숨은 rect가 동작하지 않는다.
- [ ] deep scroll에서도 탭은 고정되고 본문/tooltip이 header나 탭 아래로 새지 않는다.

## v0.5.8.2 Reset / 탭 의미 / UI 문구

- [ ] 슬라이더 숫자 입력 오른쪽의 `초기화`가 slider/input과 겹치지 않는다.
- [ ] 초기화는 해당 값 하나만 바꾸며 숫자와 thumb가 즉시 같은 값이 된다.
- [ ] Offset X/Y 초기화는 다른 축을 보존하고 Ctrl+Z/Ctrl+Y가 동작한다.
- [ ] 배경 레이어가 없거나 active index가 잘못되어도 초기화가 저장/crash하지 않는다.
- [ ] LAYERS와 JUICE & VFX는 장면·전투에만 있고 화면·배경에는 없다.
- [ ] 화면·배경은 BG/Camera/Parallax 중심이며 탭은 계속 3개뿐이다.
- [ ] 일반 화면의 대시 잔여 막대는 한 곳만 보이고 하단 Dash 키는 유지된다.
- [ ] 실제 대시 충전·소모 및 지상/공중 먼지 조건이 이전과 같다.
- [ ] 한국어 Controls에 공격/대시/점프/교대/스킬 1~3/합격기가 보인다.
- [ ] 영어 Controls는 자연스러운 영어 action label을 표시하고 키 이름은 바뀌지 않는다.
- [ ] 배경/NPC/key-map/Synergy 빈 상태가 다음 행동을 알려 준다.
- [ ] Reset과 전체 시체 삭제/레이어 삭제가 문구와 색으로 구분된다.
- [ ] 한/영 최소 창과 deep scroll에서 소제목, Reset, tooltip이 잘리지 않는다.
- [ ] 상단 순서, T 전면 교대, G corpse 제외, O 1프레임, Combo/Intro/
  Despawn/corpse snap, Partner, SporeHeart, F5 위치, parallax gizmo/history가 유지된다.

## v0.5.8.3 Icon / 중간 빌드

- [ ] 정상 Tcl/Tk가 포함된 Python 3.12 환경에서 icon spec clean build를 다시 수행한다.
- [ ] 빌드 로그에 tkinter 제외 경고가 없고 EXE `--check`가 종료 코드 0이다.
- [ ] EXE `--check-sidebar-ui`가 종료 코드 0이며 overlap/hidden count가 0이다.
- [ ] Windows 파일 탐색기에서 새 EXE 아이콘이 보인다.
- [ ] 작업 표시줄과 Alt+Tab에서 앱 아이콘이 보인다.
- [ ] ZIP 압축 해제 후 EXE가 실행되고 기본 GUI가 열린다.
- [ ] 배포 ZIP hash가 README/release note의 SHA-256과 일치한다.
- [ ] 미서명 빌드에서 Unknown Publisher/SmartScreen 경고가 표시될 수 있음을 확인한다.
- [ ] 상단 순서, OPTIONS 3탭, Reset, 번역, LAYERS/VFX, 대시 HUD,
  T 교대, G recall, Combo/Intro/corpse 및 parallax 동작을 수동 확인한다.
- [ ] 현재 `dist_v0.5.8.3_icon_final`은 smoke 실패 진단 산출물이므로 배포하지 않는다.

## v0.5.8.3.1 Tcl/Tk 수정 빌드

자동 확인 완료:

- PyInstaller 표준 hook에 `_tkinter.pyd`, Tcl/Tk DLL/data,
  `pyi_rth__tkinter.py`가 포함되고 tkinter 제외 경고가 없습니다.
- `dist_v0.5.8.3_tkfix2/ase_viewer.exe --check`와
  `--check-sidebar-ui`가 모두 종료 코드 0입니다.
- frozen check가 Tcl 8.6.12와 외부 예제 리소스 정책을 보고하며 dialog를
  열거나 프로세스를 남기지 않습니다.

사용자 수동 확인:

- [ ] 파일 탐색기에서 EXE 아이콘이 보인다.
- [ ] 실행 중 작업 표시줄과 Alt+Tab에 앱 아이콘이 보인다.
- [ ] 배포 ZIP을 새 폴더에 압축 해제한 뒤 EXE가 실행된다.
- [ ] 파일 열기/저장 dialog가 정상적으로 열리고 닫힌다.
- [ ] 동봉 `.sha256`의 값과 ZIP의 실제 SHA-256이 일치한다.
- [ ] 미서명 앱의 Unknown Publisher/SmartScreen 경고 가능성을 확인한다.
- [ ] 기본 GUI가 열리고 상단 `태그 등록 / 배치 / 리소스 / 옵션` 순서다.
- [ ] OPTIONS 3탭, slider Reset, LAYERS/VFX 위치, 한/영 조작키,
  대쉬 표시 한 세트가 유지된다.
- [ ] T 전면 교대, G corpse 제외, ComboAttack, Intro no-jump,
  corpse ground snap, parallax gizmo와 undo/redo가 유지된다.

## v0.5.8.3 회사 내부 예제 포함 패키지

자동 확인 완료:

- staging의 `resources/examples`에서 필수 예제 9/9를 탐지했습니다.
- EX1/EX2 실제 소스 준비, 리소스 해시, EX2 6개 패럴랙스 적용·렌더
  테스트가 통과했습니다.
- 한글·공백·`#` 파일명이 복사 후 보존됐습니다.
- staging EXE `--check`와 `--check-sidebar-ui`가 종료 코드 0이며 dialog나
  잔류 프로세스가 없습니다.

사용자 수동 확인:

- [ ] 새 ZIP을 완전히 새 폴더에 압축 해제한다.
- [ ] `ase_viewer.exe` 옆에 `resources/examples`가 있다.
- [ ] 명령줄 `ase_viewer.exe --check`가 예제 `9/9`를 보고한다.
- [ ] EX1과 EX2 버튼 모두 missing resources 팝업 없이 로드된다.
- [ ] 예제 로드 전 현재 프로젝트의 저장·복원 흐름이 자연스럽다.
- [ ] 파일 열기/저장 dialog, 파일 탐색기·작업 표시줄·Alt+Tab 아이콘이
  정상이다.
- [ ] 동반 `.sha256`과 ZIP 실제 SHA-256이 일치한다.
- [ ] 미서명 앱의 Unknown Publisher/SmartScreen 경고 가능성을 확인한다.
- [ ] OPTIONS 3탭, Reset, 번역, 대쉬 표시, T/G/Combo/Intro/Corpse/
  Parallax 기능이 유지된다.
